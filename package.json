{
  "scripts": {
    "dev-install": "yarn install -D",
    "start": "$(yarn bin)/parcel index.html",
    "build": "$(yarn bin)/parcel build -d public index.html"
  },
  "devDependencies": {
    "elm-hot": "^1.0.1",
    "node-elm-compiler": "^5.0.3",
    "parcel": "1.12.0",
    "sass": "^1.17.4"
  }
}
